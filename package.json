{
  "name": "qbot-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Super Personal Assistant",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
